# Mixture of Thoughts (MoT) Framework Requirements

# Core dependencies
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
tqdm>=4.65.0

# For training utilities
wandb>=0.15.0  # Optional: for experiment tracking
huggingface_hub>=0.16.0  # For model downloading
colorama>=0.4.6  # For colored terminal output

# For different model architectures
accelerate>=0.20.0  # For model parallelism and mixed precision
sentencepiece>=0.1.99  # For certain tokenizers
protobuf>=3.20.0  # For certain model formats

# For sentence encoding
sentence-transformers>=2.2.0  # For DeBERTa-v3 sentence encoder

# Optional: For loading quantized models
bitsandbytes>=0.39.0  # For 8-bit quantization support

# Development and testing
pytest>=7.3.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Data processing
datasets>=2.12.0  # HuggingFace datasets
tokenizers>=0.13.0  # Fast tokenizers

# Visualization (optional)
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

google-generativeai==0.1.0rc1